= render 'user_form', :op => 'Edit'
- if current_user != @user
  %p.form-delete-link= link_to("Delete User", @user, :method => 'delete', :confirm => 'Are you sure you want to delete this user?  This can not be undone.')