%div#user-actions.round
  %p= link_to("Create New List", new_list_path)
%table#lists-list.auto-sorting-table
  %thead
    %tr
      %th Name
      %th Description
      %th School
  %tbody
    - @lists.each do |list|
      - if can? :read, list
        %tr
          %td= link_to list.name, list
          %td= list.description
          %td= list.school.name unless list.school.nil?