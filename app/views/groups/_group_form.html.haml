= form_for(@group) do |f|
  = render 'shared/error_messages', :object => f.object
  %fieldset
    %legend Basic Info
    %div.form-field
      = f.label :name, :class => "required"
      = f.text_field :name
  %div.form-submit
    = f.submit "#{op} Group"