%p.back-link= link_to "Back", groups_path
= render 'group_form', :op => 'Edit'
%p.form-delete-link= link_to("Delete Group", @group, :method => 'delete', :confirm => 'Are you sure you want to delete this group?  This can not be undone.')